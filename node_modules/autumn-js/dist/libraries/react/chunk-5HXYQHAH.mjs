"use client";

import {
  BaseAutumnProvider
} from "./chunk-V4RYP3EY.mjs";
import {
  AutumnContext
} from "./chunk-4XXJ4V7H.mjs";
import {
  ConvexAutumnClient
} from "./chunk-32BXVAYB.mjs";
import {
  AutumnClient
} from "./chunk-IXIFYTKC.mjs";

// src/libraries/react/ReactAutumnProvider.tsx
import { jsx } from "react/jsx-runtime";
var getBackendUrl = (backendUrl) => {
  if (backendUrl) {
    return backendUrl;
  }
  if (backendUrl && !backendUrl.startsWith("http")) {
    console.warn(`backendUrl is not a valid URL: ${backendUrl}`);
  }
  return "";
};
var ReactAutumnProvider = ({
  children,
  getBearerToken,
  convex,
  backendUrl,
  customerData,
  includeCredentials,
  betterAuthUrl,
  headers,
  convexApi,
  pathPrefix
}) => {
  let client = convexApi ? new ConvexAutumnClient({
    convex,
    convexApi,
    customerData,
    headers,
    getBearerToken
  }) : new AutumnClient({
    backendUrl: getBackendUrl(backendUrl),
    getBearerToken,
    customerData,
    includeCredentials,
    betterAuthUrl,
    headers,
    pathPrefix
  });
  return /* @__PURE__ */ jsx(BaseAutumnProvider, { client, AutumnContext, children });
};

export {
  ReactAutumnProvider
};
